{% extends '/common/template.php.twig' %}

{% block title %}
	<title>User account</title>
{% endblock %}

{% block content %}

	<div class="container-xl">
		<div class="example-wrapper">
			<h1 class="mt-3 mb-3 back-h1 text-center">User Account</h1>

			<h2 class="text-center">{{ user.username }}</h2>

			{% if user.avatarImg is same as null %}

				{% else %}
					<div class="d-flex justify-content-center mt-3 mb-3">
						<img class="img-thumbnail rounded" style="height:180px;" src="{{ asset('media/user/' ~ user.avatarImg) }}" alt="">
					</div>
			{% endif %}
			<div class="container pt-5">
				<div class="row" id="flash-row">
					<div class="col" id="flash-col">
						{% for rubrique, messages in app.flashes %}
							<div class="alert alert-{{ rubrique }} alertr-dismissible fade-show" id="success-flash" role="alert">
								{% for message in messages %}
									<p>{{ message }}</p>
								{% endfor %}
								<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
							</div>
						{% endfor %}
					</div>
				</div>
			</div>

			<p class="text-center">In this space, you will have the possibility to add a photo which will be your avatar image.
		</br>Your avatar image will be visible to all users when you post comments.</p>
</div>


{{ form_start(form) }}

{{ form_errors(form) }}

{{ form_row(form.avatarImg) }}

<div class="d-flex justify-content-center mt-3 mb-3">
	<button class="btn btn-lg btn-primary sign-form-btn" type="submit">
		Update
	</button>
</div>

{{ form_end(form) }}


<p class="text-center">If you want to change your password, you can do so by following this
	<a href="{{ path('app_forgot_password_request') }}">link</a>.</p></div>{% endblock %}{% block footer %}<footer style="background-color:#02335b; color:#fff;" class="footer-big bg-gradient fixed-bottom">
<div class="container-fluid">
	<div class="row pt-4">
		<div class="col-12 text-center">
			<p>Copyright ©
				{{ "now"|date("Y") }}
				Snowtricks |
				<a class="blog-link" href="https://www.coyotech.fr/blog-mvc/" target="_blank">CoyoTech.fr</a>
			</p>
		</div>
	</div>
</div></footer>{% endblock %}
